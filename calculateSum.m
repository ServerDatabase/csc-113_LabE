function add = calculateSum(input1, input2)
    % This function adds two numbers.
    % Inputs: input1, input2 - numbers to add
    % Output: add - the sum of the two inputs
    
    add = input1 + input2; %add inputs
end
